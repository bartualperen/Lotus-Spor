<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Lotus_Spor.AdminPanelPage"
             Title="AdminPanelPage">
    <VerticalStackLayout Padding="20">
        <Label Text="Üye Listesi" FontSize="24" HorizontalOptions="Center" />
        <CollectionView x:Name="MemberList">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding Name}" FontSize="18" />
                        <Button Text="Detaylar" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="Yeni Üye Ekle" Margin="10,10,5,10"/>
        <Button Text="Çıkış Yap" Margin="10,10,5,10" Clicked="OnClickedLogOut" x:Name="LogOutButton" />
    </VerticalStackLayout>
</ContentPage>